Atomic Level: Organism

Reasoning:
- Organisms are complex components composed of molecules and atoms (e.g., header, card list, forms).
- Use for larger, layout-level components that encapsulate behaviour and structure.

Usage:
- Create one folder per organism, e.g. `Header/` with `Header.tsx`, `Header.css`.

Policy note:
- Follow BEM naming and semantic HTML as defined in .github/agents/design-system-architect-agent.agent.md
